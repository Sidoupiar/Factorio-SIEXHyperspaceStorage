-- ------------------------------------------------------------------------------------------------
-- ---------- 解锁配方 ----------------------------------------------------------------------------
-- ------------------------------------------------------------------------------------------------

for forceName , force in pairs( game.forces ) do
	for recipeName , recipe in pairs( force.recipes ) do
		if recipeName:sub( 1 , 7 ) == "siexhs-" then recipe.enabled = true end
	end
end